// Simple test to verify jest and the basic route work
import { describe, expect, test } from '@jest/globals';

describe('Basic Jest Test', () => {
  test('should work', () => {
    expect(1 + 1).toBe(2);
  });
});
