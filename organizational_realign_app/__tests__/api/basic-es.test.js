// __tests__/api/basic-es.test.js
import { describe, it, expect } from '@jest/globals';

describe('Basic ES Module Test', () => {
  it('should add numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
